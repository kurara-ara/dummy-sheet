import MessageBus from "../messages/MessageBus";
import { Metadata } from "../types";
import { Tool, ToolAction, ToolMode } from "../types/Tool";
declare class ToolApi {
    private messageBus;
    private tools;
    private toolActions;
    private toolModes;
    constructor(messageBus: MessageBus);
    private handleToolClick;
    private handleToolActionClick;
    private handleToolModeClick;
    private handleToolModeToolClick;
    private handleToolModeToolDoubleClick;
    private handleToolModeToolDown;
    private handleToolModeToolMove;
    private handleToolModeToolUp;
    private handleToolModeToolDragStart;
    private handleToolModeToolDragMove;
    private handleToolModeToolDragEnd;
    private handleToolModeToolDragCancel;
    private handleToolModeKeyDown;
    private handleToolModeKeyUp;
    create(tool: Tool): Promise<void>;
    remove(id: string): Promise<void>;
    activateTool(id: string): Promise<void>;
    getMetadata(id: string): Promise<Metadata | undefined>;
    setMetadata(toolId: string, update: Partial<Metadata>): Promise<void>;
    createAction(action: ToolAction): Promise<void>;
    removeAction(id: string): Promise<void>;
    createMode(mode: ToolMode): Promise<void>;
    removeMode(id: string): Promise<void>;
    activateMode(toolId: string, modeId: string): Promise<void>;
}
export default ToolApi;
